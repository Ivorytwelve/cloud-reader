# Cloud Reader branding/header patch

- Adds the blue Ttsu/Cloud Reader icon before the `Cloud Reader` title.
- Uses the GitHub-Pages-safe `basePath` for the image URL.
- Uses the 32x32 regular icon at 28 CSS px.
- Includes the favicon/manifest cache-busting changes from the previous patch.
- Keeps the browser theme color at `#90CAF9`.

This patch supersedes the separate favicon-cache-bust patch.
